# adaptive-olmc — 实现标准 (Implementation Standard)

本文件在每个会话自动加载。要新增功能时,只需说:

> 按 CLAUDE.md 的实现标准,给我加【功能】。

以下标准适用于本 repo 的**所有** implement / 改代码任务,除非我在具体请求里明确豁免某条。

---

## 1. 功能正确

- 完整实现所要求的功能,**别多做我没要求的(no scope creep)**;需求不确定时**先问,不要猜着加东西**。
- **交付前实测**:跑通、给我看**真实输出**,不靠"应该能行"。
- 涉及**无损 / 往返 / 确定性**的改动,**必须用真实数据端到端验证**(round-trip 逐比特一致、编解码两端状态同步),不只测编译或单测。

## 2. 完美兼容,不引入问题

- 动手前**先读懂相关既有代码**;复用已有的抽象 / 工具 / 风格,不重复造轮子。
- **只加不改**:除非必要,不动既有函数签名和行为。万一要改,**明确告诉我改了什么、为什么、影响哪些调用点**。
- 改完确认**既有功能没坏**(相关测试 / 入口 / pipeline 仍通)。

## 3. 风格与既有代码一致

- 注释、docstring、命名、输出格式、错误处理——**全部对齐本 repo 现有惯例**(先看邻近文件怎么写,跟着来),不要引入你自己的风格。
- 注释解释**"为什么 / 约束"**,不复述"做了什么"。
- 控制台输出**工整对齐、简洁清晰**(对齐本 repo 现有的打印格式)。

## 4. 结构与工程质量(对标业界主流顶级 repo)

- 逻辑清晰、职责单一、分层合理;**纯逻辑与 I/O / 副作用分离**,便于测试和复用。
- pipeline 顺畅:默认值合理、**fail-fast**(在浪费算力 / 时间前就报错)、产物落在合理位置。
- 关键**纯逻辑配轻量测试**;不留死代码、调试残留、临时文件。

---

## 交付时必须告诉我

1. **改了哪些文件**;
2. **每个文件做了什么**;
3. **怎么验证的**(附**真实输出**);
4. **有没有动到既有行为**(动了就逐点说明)。
